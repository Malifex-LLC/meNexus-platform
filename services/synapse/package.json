{
  "name": "@menexus/synapse",
  "version": "0.3.0-alpha.0",
  "description": "Rust backend for meNexus",
  "private": true,
  "scripts": {
    "build": "cargo build --release",
    "dev": "cargo run",
    "test": "cargo test",
    "lint": "cargo clippy",
    "typecheck": "cargo check",
    "clean": "cargo clean"
  }
}
